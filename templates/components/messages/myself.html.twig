{% set message = message|default('') %}
{% set time = time|default('') %}

<div class="flex flex-col items-end pr-2 space-y-1">
	<div class="py-2 px-3 rounded-full bg-purple-200 w-fit max-w-2xs">
		<p class="text-xs text-purple-700">{{ message }}</p>
	</div>
    <p class="text-2xs text-lavande-400">{{ time }}</p>
</div>
